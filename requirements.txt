# Python dependencies for running the DevOps study notebooks
# Install with: pip install -r requirements.txt

# Core utilities
pyyaml>=6.0           # YAML parsing (workflow analysis)
rich>=13.0            # Beautiful terminal output

# Async support (already in Python 3.11+, but for older versions)
asyncio                # Built-in; listed for documentation

# Visualization (for diagram notebooks)
matplotlib>=3.7
networkx>=3.1          # Graph visualization (pipeline DAGs)

# Kubernetes client (for K8s module examples)
kubernetes>=27.0

# Ansible (for module 05)
ansible>=8.0
ansible-lint>=6.0
molecule>=6.0

# Terraform helpers
# terraform is a binary; install via tfenv or direct download
python-hcl2>=4.0       # Parse Terraform HCL in Python

# AWS SDK (for Terraform/cloud module examples)
boto3>=1.28

# Prometheus client (for monitoring module)
prometheus-client>=0.17

# Structured logging
structlog>=23.0

# Testing
pytest>=7.4
pytest-asyncio>=0.21

# Security scanning helpers
detect-secrets>=1.4    # Secret detection in files
