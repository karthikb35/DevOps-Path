# Development and CI dependencies
# Install with: pip install -r requirements-dev.txt

# ── Linting & formatting ──────────────────────────────────────────────────
ruff>=0.5.0            # all-in-one linter + formatter (replaces flake8, isort, bandit, black)

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7.4
pytest-cov>=4.1

# ── YAML processing (used by protection tests) ────────────────────────────
pyyaml>=6.0
yamllint>=1.35

# ── Pre-commit (optional — run pre-commit install after this) ─────────────
pre-commit>=3.7

# ── PDF rendering (for render_pdf.py) ─────────────────────────────────────
playwright>=1.44       # headless Chromium; run: python -m playwright install chromium
